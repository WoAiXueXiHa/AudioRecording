# 示例文件不会自动加载；当前通过 shell 环境变量配置。
# 未设置时默认监听 127.0.0.1:8080。
HTTP_ADDR=127.0.0.1:8080
# 先创建数据库并执行 migrations/001_init.sql，再启动服务。
MYSQL_DSN=audio:replace_me@tcp(127.0.0.1:3306)/audio_recording
UPLOAD_DIR=./uploads
