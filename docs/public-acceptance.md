# 公网部署验收

验收日期：2026-09-12。服务地址：`http://192.144.168.226:8080`。

本次使用服务器原有镜像和真实 DeepSeek 配置，将 API 端口由回环地址改为 `0.0.0.0:8080`，保留原数据库和上传卷。MySQL 不发布宿主机端口。服务器旧配置备份为 `~/audio-recording/compose.before-public-20260912.yaml`。

从本地通过公网访问，未使用 SSH 隧道或摘要替身：

| 检查 | 结果 |
| --- | --- |
| GET /health | 200，status=ok |
| 上传 MP3 | 201，返回 pending；本次请求约0.189秒 |
| 任务状态 | pending → transcribing → summarizing → done |
| 真实 DeepSeek 摘要 | 返回非空 summary、5条 key_points、3条 todos |
| 处理中删除 | 409 |
| done 任务重试 | 409 |
| 分页与录音详情 | 200，详情含转写和摘要 |
| 删除本次验收录音 | 204；随后录音与任务查询均404 |

本次 recording_id=2、task_id=2，没有触发 Mock 失败或手动重试，验收数据已清理。摘要示例：“团队讨论了录音转写服务的交付安排，明确分阶段完成上传与查询、智能摘要，并分配了验证和文档工作，计划周五前验收。”

转写仍为题目允许的 Mock，不代表识别了音频真实内容。本次验证真实 LLM 调用及公网主链路，不代表长期可用性或负载测试。状态机、异常与并发测试见 `scripts/check-all.py`。
